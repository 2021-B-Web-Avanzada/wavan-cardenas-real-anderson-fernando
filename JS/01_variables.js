// Variables Mutables o inmutables

// Mutables
var numeroUno = 1;
let numeroDos = 2;

// Inmutables
const configuracionArchivo = 'PDF';

// De preferencia siempre usar let y const

// Tipos de variables

const numero = 1;
const sueldo = 1.2;
const texto = 'Hola';
const apellido = 'Cárdenas';
const booleano = false;
const hijo = null;
const zapato = undefined;
//console.log(typeof(Number(value:'adf')));
console.log(typeof (texto));
console.log(typeof (hijo));
console.log(typeof (zapato));
